{
  "name": "custom-html-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "Plain HTML/CSS/JS deck scaffold using the platform custom runtime.",
  "scripts": {
    "preview": "echo \"Open this deck through the platform /runtime/:id route.\""
  }
}
